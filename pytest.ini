[pytest]
markers =
    integration: marks tests as integration tests, which may require external services.
    slow: marks tests as slow-to-run, moving them to the end of the queue.

order_scope = session

# This defines the explicit, tiered execution order.
order_groups =
    not slow and not integration
    slow
    integration